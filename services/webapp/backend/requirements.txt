fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.9
supabase>=2.10
pydantic-settings>=2.6

# Image conversion (same pattern as services/uploader). HEIC needs libheif
# but the import itself is optional and the upload route degrades gracefully.
Pillow>=10.0
pillow-heif>=0.16

# Test runner — not required at runtime but pinned here so 'pip install -r
# requirements.txt' on a dev machine sets up the full toolchain.
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.28
