# No backend URL by default — apps/web/src/app/page.tsx derives it from
# window.location.hostname at runtime. Set this only if the backend is on
# a different host than the browser (e.g. behind a Tailscale Funnel).
#
# NEXT_PUBLIC_BACKEND_URL=https://harcourts-mac.tail-xxx.ts.net
